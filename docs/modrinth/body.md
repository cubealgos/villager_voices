# Wait, they talk now?

Villagers react to what happens to them, with voices and text.

The body itself is a later ticket's territory (`docs/spec/operations/release.md`). Project settings:

| Setting | Value |
|---|---|
| Title | Wait, they talk now? |
| Slug | `villager-voices-mod` — **provisional**: the proposed slug `villager-voices` is already taken by an unrelated resource pack; this alternative is not yet confirmed (`docs/spec/decisions/DEC-002-name.md`) |
| License | MIT |
| Client/server | Both required |
