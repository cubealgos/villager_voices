<!-- generated by tools/map.py; edit the source, then run `just map` -->

# `villager_voices.compat` (test) — common

Every type with its summary and every non-private constructor, method and constant. The
signature is the contract; read the source only when the summary is not enough.

### `class TalkingStateTest` — `common/src/test/java/villager_voices/compat/TalkingStateTest.java`
docs/spec/operations/testing.md's unit layer, COMPAT-REQ-002: a villager is talking for exactly the duration a line's sound plays, nothing before or after — pure, no fake clock needed since now is just a caller-supplied long (VV-2's own ReactionRules test pattern).
- `void aVillagerNeverMarkedIsNeverTalking()`
- `void isTalkingThroughEveryTickStrictlyBeforeUntilTick()`
- `void isNotTalkingFromUntilTickOnward()`
- `void aRepeatStartTalkingReplacesRatherThanExtendsTheMark()`
- `void eachVillagerIdIsTrackedIndependently()`

