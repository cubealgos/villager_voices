<!-- generated by tools/map.py; edit the source, then run `just map` -->

# `villager_voices.fabric.config` (test) — fabric

Every type with its summary and every non-private constructor, method and constant. The
signature is the contract; read the source only when the summary is not enough.

### `class ConfigLoaderTest` — `fabric/src/test/java/villager_voices/fabric/config/ConfigLoaderTest.java`
"the config file is written on first start with defaults" (this ticket's own test list), and the read side of docs/spec/contracts/data-contract.md DATA-REQ-002/DATA-REQ-004 — all plain file I/O against a JUnit temp directory, no game instance needed.
- `void firstRunWritesTheShippedDefaultsAndReturnsThem(Path configDir)`
- `void aSecondLoadReadsBackExactlyWhatTheFirstRunWrote(Path configDir)`
- `void aCustomisedFileOnDiskIsHonouredInsteadOfBeingOverwritten(Path configDir)`
- `void aMalformedFileDegradesToDefaultsAndIsLeftUntouchedOnDisk(Path configDir)`

