<!-- generated by tools/map.py; edit the source, then run `just map` -->

# `villager_voices.fabric.mixin` — fabric

Every type with its summary and every non-private constructor, method and constant. The
signature is the contract; read the source only when the summary is not enough.

### `class BabyGrowsMixin` — `fabric/src/main/java/villager_voices/fabric/mixin/BabyGrowsMixin.java`
baby_grows (docs/spec/domains/reaction.md §3): injects Villager#ageBoundaryReached() at TAIL — fires exactly once, at the moment a baby becomes an adult (REACTION-REQ-010's one exemption), narrower than hooking the shared aging tick.

### `class BellRingMixin` — `fabric/src/main/java/villager_voices/fabric/mixin/BellRingMixin.java`
raid_bell, ring target (docs/spec/domains/reaction.md §3): injects BellBlockEntity#onHit(Direction) at TAIL.

### `class BreedingMixin` — `fabric/src/main/java/villager_voices/fabric/mixin/BreedingMixin.java`
breeding (docs/spec/domains/reaction.md §3): injects Villager#getBreedOffspring(ServerLevel, AgeableMob) at TAIL, filtered to a non-null return (the vanilla caller, VillagerMakeLove.breed, treats null as "breeding did not happen" and never spawns a child).

### `class LevelUpMixin` — `fabric/src/main/java/villager_voices/fabric/mixin/LevelUpMixin.java`
level_up (docs/spec/domains/reaction.md §3): injects Villager#setVillagerData(VillagerData) at HEAD, comparing the villager's current level (read before the new data is applied) against the incoming data's level; publishes a LEVEL_UP signal only when the level actually increases.

### `class RaidStartMixin` — `fabric/src/main/java/villager_voices/fabric/mixin/RaidStartMixin.java`
raid_bell, raid-start target (docs/spec/domains/reaction.md §3): injects Raids#createOrExtendRaid(ServerPlayer, BlockPos) at TAIL, using the triggering player's own level (the Raids instance itself holds no net.minecraft.world.level.Level reference of its own — confirmed by javap) and the given pos to reach every nearby villager.

### `class RestockMixin` — `fabric/src/main/java/villager_voices/fabric/mixin/RestockMixin.java`
restock (docs/spec/domains/reaction.md §3): injects Villager#restock() at TAIL.

### `class TradeCompletedMixin` — `fabric/src/main/java/villager_voices/fabric/mixin/TradeCompletedMixin.java`
trade_completed (docs/spec/domains/reaction.md §3): injects AbstractVillager#notifyTrade(MerchantOffer) at TAIL, publishing a TRADE_COMPLETED signal without observing or altering the method's own effect (notifyTrade returns void; this mixin reads nothing from and writes nothing to offer or this) — this ticket's TEST-REQ-003 obligation for this event.

