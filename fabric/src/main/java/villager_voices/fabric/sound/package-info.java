/**
 * The 64 registered {@code SoundEvent}s (docs/spec/domains/audio.md, {@code AUDIO-REQ-001}), the
 * fabric-side {@code ServerLevel.playSeededSound} call, and the {@code LineSink} adapter that ties
 * VV-2's reaction pipeline, VV-3's catalogue, and VV-7's display queue into an actually
 * playing/displaying reaction (VV-8).
 */
package villager_voices.fabric.sound;
