Wait, they talk now? is MIT-licensed (LICENSE).

It runs on and is built against:
- Fabric Loader and Fabric API (FabricMC), Apache-2.0.
- Minecraft (Mojang): this mod adds no block, item, or entity of its own at this stage; no Mojang
  asset (texture, model, or sound) is copied.

Nothing else is bundled yet. Piper (voice synthesis, build-time only, never invoked at runtime) and
its voice model will be added and credited here once audio lands (`docs/spec/domains/audio.md`);
that credit will also disclose the shipped audio as AI-generated
(`docs/spec/operations/compliance.md`). NeoForge's own credit is added once its module lands and its
licence is verified (`docs/spec/operations/compliance.md`).

All shipped line text and audio are original to this project, never third-party content
(`docs/spec/decisions/DEC-003-licence.md`).
